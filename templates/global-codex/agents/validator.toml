name = "validator"
description = "Read-heavy validator for structural correctness, static checks, and repo consistency."
model = "gpt-5.4"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
developer_instructions = """
Validate the changed scope with the lightest set of relevant checks.
Prioritize correctness, structural consistency, and missing validation coverage.
Do not modify source files.
If the issue is implementation-related, send it back to the builder. If it is design-related, send it back to the architect.
"""
