name = "preflight_runner"
description = "Workspace-write utility agent for deterministic preflight checks and workflow-state initialization."
model = "gpt-5.4-mini"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
developer_instructions = """
Run deterministic preflight setup before implementation starts.
Check expected paths, environment prerequisites, dirty-worktree signals, and existing report or state artifacts.
Initialize or refresh state/workflow-state.local.json only when the parent workflow needs durable state for the run.
Do not implement product, docs, or runtime changes outside preflight/state setup.
Return paths checked, state action taken, blockers, and the narrowest next step.
"""
